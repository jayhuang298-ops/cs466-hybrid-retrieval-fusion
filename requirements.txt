# ============================================================
# Hybrid Retrieval Fusion — Python dependencies
# Platform: Apple Silicon (M1/M2/M3/M4), Python 3.11
# ============================================================
# Install order matters — see scripts/01_setup_env.sh
# faiss-cpu is installed via conda, not pip (more stable on ARM)
# ============================================================

# --- Core retrieval ---
pyserini==0.22.1
sentence-transformers==2.7.0
transformers>=4.40.0

# --- PyTorch (MPS backend — installed separately via pip, see setup script) ---
# torch>=2.1.0   <-- do NOT pip install here; handled in setup script

# --- BEIR & evaluation ---
beir==2.0.0
pytrec-eval-terrier>=0.5.6   # maintained fork; compiles on ARM64

# --- ML / data ---
scikit-learn>=1.3.0
numpy>=1.24,<2.0             # numpy 2.x can break pyserini/beir on 3.11
pandas>=2.0
scipy>=1.11

# --- Utilities ---
tqdm
pyyaml
nltk                          # stopword list for Strategy E query features
matplotlib>=3.8
seaborn>=0.13
jupyterlab>=4.0
